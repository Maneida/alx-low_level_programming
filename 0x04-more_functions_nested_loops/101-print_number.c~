#include "main.h"

/**
 * print_number - prints integer
 * @n: integer to be printed
 *
 * Return: no return
 */

void print_number(int n)
{
	if (n != 0)
		_putchar('0' + (n % 10));
	else
		_putchar('0');
}
